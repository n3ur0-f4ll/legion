# Legion — zależności projektu
# Instalacja: pip install -r requirements.txt

# Kryptografia
pynacl>=1.5

# Sieć Tor
stem>=1.8

# Sieć asynchroniczna
websockets>=12.0

# API lokalne (dla GUI)
fastapi>=0.111
uvicorn>=0.29

# Baza danych
aiosqlite>=0.20

# Dev / testy
pytest>=8.0
pytest-asyncio>=0.23
