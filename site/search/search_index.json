{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Legion \u2014 Technical Overview","text":"<p>Legion is a decentralized, anonymous messaging platform built on the Tor network. Users run their own nodes; there are no central servers, no accounts, and no phone numbers. Every message is end-to-end encrypted with libsodium (PyNaCl) and delivered peer-to-peer through Tor Hidden Services.</p>"},{"location":"#component-architecture","title":"Component architecture","text":"<pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  User's machine                                             \u2502\n\u2502                                                             \u2502\n\u2502  \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510   HTTP/SSE    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510    \u2502\n\u2502  \u2502  legion-gui  \u2502 \u25c4\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba  \u2502    legion-node       \u2502    \u2502\n\u2502  \u2502  (pywebview) \u2502  127.0.0.1    \u2502  FastAPI + uvicorn   \u2502    \u2502\n\u2502  \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518   :8080       \u2502                      \u2502    \u2502\n\u2502                                 \u2502  WebSocket server    \u2502    \u2502\n\u2502                                 \u2502  127.0.0.1:8765      \u2502    \u2502\n\u2502                                 \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518    \u2502\n\u2502                                            \u2502 SOCKS5         \u2502\n\u2502                                    \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25bc\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510       \u2502\n\u2502                                    \u2502  Tor process   \u2502       \u2502\n\u2502                                    \u2502  (subprocess)  \u2502       \u2502\n\u2502                                    \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518       \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u253c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n                                             \u2502 Tor network\n                               \u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25bc\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n                               \u2502  Other Legion nodes        \u2502\n                               \u2502  (contact's .onion)        \u2502\n                               \u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"#communication-paths","title":"Communication paths","text":"Path Protocol Purpose <code>legion-gui</code> \u2194 <code>legion-node</code> HTTP REST + SSE GUI sends commands, node pushes live events <code>legion-node</code> \u2192 remote node WebSocket over Tor SOCKS5 Outgoing message delivery Remote node \u2192 <code>legion-node</code> WebSocket via Tor HS Incoming message reception <code>legion-node</code> \u2192 <code>legion-relay</code> WebSocket over Tor (optional) Relay-assisted delivery"},{"location":"#technology-stack","title":"Technology stack","text":"Component Key libraries <code>legion-node</code> FastAPI, uvicorn, aiosqlite, PyNaCl, Stem, websockets <code>legion-gui</code> pywebview (GTK/WebKit2), PyGObject <code>legion-relay</code> websockets, aiosqlite, Stem, PyNaCl <p>Python: \u2265 3.12 \u00b7 OS: Linux only \u00b7 License: AGPL-3.0</p>"},{"location":"#startup-sequence","title":"Startup sequence","text":"<ol> <li><code>legion-gui/app/main.py</code> spawns <code>legion-node/main.py --no-interactive</code></li> <li>Node opens SQLite database, applies schema migrations</li> <li>Node starts WebSocket server on <code>127.0.0.1:8765</code></li> <li>Node starts delivery queue background loop</li> <li>Node starts FastAPI/uvicorn on <code>127.0.0.1:8080</code></li> <li>GUI polls <code>GET /api/status</code> until node responds</li> <li>GUI opens pywebview window \u2014 user sees Unlock or Onboarding screen</li> <li>User enters password \u2192 <code>POST /api/identity/unlock</code></li> <li>Node decrypts private key, stores in <code>AppState.identity</code></li> <li>Node launches Tor as background task, creates v3 Hidden Service</li> <li>Tor emits <code>tor_ready</code> \u2192 SSE pushes to GUI \u2192 status bar turns green</li> </ol>"},{"location":"gui/overview/","title":"legion-gui \u2014 Overview","text":"<p><code>legion-gui</code> is the desktop interface for Legion. It is built with pywebview (GTK/WebKit2 backend on Linux) and renders a vanilla HTML/CSS/JS single-page application inside a native window. It acts as a launcher for <code>legion-node</code> \u2014 the user only runs <code>legion-gui</code>.</p>"},{"location":"gui/overview/#architecture","title":"Architecture","text":"<pre><code>python3 legion-gui/app/main.py\n          \u2502\n          \u251c\u2500 spawns \u2500\u2500\u25ba legion-node/main.py --no-interactive\n          \u2502              stdin=DEVNULL  stdout/stderr=node.log\n          \u2502              (subprocess, log \u2192 ~/.local/share/legion/node.log)\n          \u2502\n          \u251c\u2500 waits \u2500\u2500\u2500\u25ba GET /api/status (polls until HTTP 200)\n          \u2502\n          \u2514\u2500 opens \u2500\u2500\u2500\u25ba pywebview window\n                         \u251c\u2500\u2500 http_server=True   (Bottle serves ui/ directory)\n                         \u251c\u2500\u2500 gui=\"gtk\"\n                         \u2514\u2500\u2500 js_api=LegionBridge\n                               \u2502\n                               \u251c\u2500\u2500 GET /api/*  \u2500\u2500\u2500\u2500\u25ba FastAPI (localhost:8080)\n                               \u2514\u2500\u2500 SSE /api/events  (live updates)\n</code></pre> <p>The node subprocess has <code>stdin=DEVNULL</code> and <code>stdout/stderr</code> redirected to a log file. This prevents node output from appearing in the user's terminal and avoids terminal state corruption.</p>"},{"location":"gui/overview/#appmainpy","title":"app/main.py","text":"<p>Entry point. Orchestrates startup and shutdown.</p>"},{"location":"gui/overview/#functions","title":"Functions","text":""},{"location":"gui/overview/#main-none","title":"<code>main() \u2192 None</code>","text":"<ol> <li>Parses CLI args (<code>--api-port</code>, <code>--debug</code>)</li> <li>Checks if node is already running (<code>_wait_for_node(timeout=1)</code>) \u2014 attaches if so</li> <li>Calls <code>_start_node()</code> and waits up to 30 seconds for it to become ready</li> <li>Sets GTK app name and application name via <code>GLib.set_prgname(\"legion\")</code> /    <code>GLib.set_application_name(\"Legion\")</code> \u2014 must be called before <code>create_window</code>    so the Wayland compositor identifies the app correctly (taskbar icon)</li> <li>Installs XDG resources if logo is present</li> <li>Creates pywebview window with <code>LegionBridge</code> as <code>js_api</code></li> <li>Registers <code>on_closed</code> callback to terminate the node when window closes</li> <li>Calls <code>webview.start(http_server=True, gui=\"gtk\", func=_apply_gtk_icon)</code></li> <li>After <code>start()</code> returns (window closed): waits up to 5 seconds for node to exit,    then kills if still running</li> <li>Calls <code>os._exit(0)</code> \u2014 intentional, bypasses normal Python shutdown to kill the     Bottle HTTP server thread that pywebview creates internally and which does not     terminate via normal <code>sys.exit()</code></li> </ol>"},{"location":"gui/overview/#_start_nodeapi_port-subprocesspopen-none","title":"<code>_start_node(api_port) \u2192 subprocess.Popen | None</code>","text":"<p>Spawns <code>legion-node/main.py --no-interactive --api-port={api_port}</code>:</p> <pre><code>subprocess.Popen(\n    [sys.executable, str(_NODE_MAIN), \"--no-interactive\", f\"--api-port={api_port}\"],\n    cwd=str(_NODE_DIR),\n    stdin=subprocess.DEVNULL,\n    stdout=log_file,\n    stderr=log_file,\n)\n</code></pre> <p>Log file: <code>~/.local/share/legion/node.log</code>. The parent closes its copy of <code>log_file</code> after <code>Popen()</code> \u2014 the child retains its own file descriptor (standard Unix fork behaviour).</p>"},{"location":"gui/overview/#_wait_for_nodeapi_port-timeout-bool","title":"<code>_wait_for_node(api_port, timeout) \u2192 bool</code>","text":"<p>Polls <code>GET http://127.0.0.1:{api_port}/api/status</code> every 300ms until a successful response or timeout. Returns <code>True</code> if the node became reachable within the timeout.</p>"},{"location":"gui/overview/#_install_xdg_resourcesicon_path-none","title":"<code>_install_xdg_resources(icon_path) \u2192 None</code>","text":"<p>Installs the application icon and <code>.desktop</code> file to <code>~/.local/share/</code> on every launch. This is idempotent \u2014 safe to call repeatedly.</p> <ul> <li>Copies <code>icon_path</code> to <code>~/.local/share/icons/hicolor/256x256/apps/legion.png</code></li> <li>Runs <code>gtk-update-icon-cache</code> (best-effort, failure ignored)</li> <li>Writes <code>~/.local/share/applications/legion.desktop</code></li> </ul> <p>Must be called before <code>create_window</code> so the Wayland compositor has the icon available when the window first appears.</p>"},{"location":"gui/overview/#_apply_gtk_icon-none","title":"<code>_apply_gtk_icon() \u2192 None</code>","text":"<p>Called via <code>func=</code> parameter of <code>webview.start()</code> \u2014 runs after the GTK main loop starts. Sets the window icon via <code>Gtk.Window.set_default_icon()</code> as an additional path for environments that don't pick up the XDG icon immediately.</p>"},{"location":"gui/overview/#appbridgepy","title":"app/bridge.py","text":"<p>The pywebview JS/Python bridge. Exposed as <code>window.pywebview.api</code> in the browser context. All methods are called from JavaScript and execute in the Python process.</p>"},{"location":"gui/overview/#legionbridge-class","title":"<code>LegionBridge</code> (class)","text":""},{"location":"gui/overview/#get_api_port-int","title":"<code>get_api_port() \u2192 int</code>","text":"<p>Returns the API port. Called once at boot from JavaScript to configure the base URL for all API calls.</p>"},{"location":"gui/overview/#get_version-str","title":"<code>get_version() \u2192 str</code>","text":"<p>Reads and returns the content of the <code>VERSION</code> file from the monorepo root. Displayed in the sidebar as a version label.</p>"},{"location":"gui/overview/#copy_to_clipboardtext-str-bool","title":"<code>copy_to_clipboard(text: str) \u2192 bool</code>","text":"<p>Copies <code>text</code> to the system clipboard. Detects Wayland vs X11 automatically:</p> <ol> <li>Tries <code>wl-copy</code> (Wayland)</li> <li>Falls back to <code>xclip -selection clipboard</code> or <code>xsel --clipboard --input</code> (X11)</li> </ol> <p>Returns <code>True</code> on success, <code>False</code> if no clipboard tool is available. Runs synchronously (subprocess call) \u2014 acceptable for clipboard operations.</p>"},{"location":"gui/overview/#save_filebase64_data-str-filename-str-str","title":"<code>save_file(base64_data: str, filename: str) \u2192 str</code>","text":"<p>Saves a base64-encoded file to <code>~/Downloads/</code>. Returns the full path, or <code>\"\"</code> on failure.</p> <ol> <li>Sanitizes filename with <code>re.sub(r'[/\\\\:*?\"&lt;&gt;|]', \"_\", filename)</code> \u2014 replaces all    path-unsafe characters, falls back to <code>\"file\"</code> if result is empty</li> <li>Creates <code>~/Downloads/</code> if it does not exist</li> <li>Avoids overwriting existing files by appending a counter (<code>photo_1.jpg</code>, <code>photo_2.jpg</code>)</li> <li>Writes decoded bytes and returns the final path, or <code>\"\"</code> on any exception</li> </ol>"},{"location":"gui/overview/#show_notificationtitle-str-message-str-none","title":"<code>show_notification(title: str, message: str) \u2192 None</code>","text":"<p>Sends a desktop notification via <code>notify-send</code>. Best-effort \u2014 failure is silently ignored.</p>"},{"location":"gui/overview/#ui-frontend","title":"ui/ \u2014 Frontend","text":"<p>Single-page application: one <code>index.html</code>, one <code>style.css</code>, one <code>app.js</code>. No build tools, no frameworks, no npm. Served by pywebview's embedded Bottle HTTP server.</p>"},{"location":"gui/overview/#startup-sequence-in-javascript","title":"Startup sequence in JavaScript","text":"<pre><code>DOMContentLoaded\n    \u2514\u2500 if window.pywebview: boot()\n       else: window.addEventListener(\"pywebviewready\", boot)\n               \u2502\n               \u25bc\n             boot()\n               \u251c\u2500 API_PORT = await window.pywebview.api.get_api_port()\n               \u2514\u2500 initApp()\n                     \u251c\u2500 GET /api/status\n                     \u251c\u2500 identity_loaded \u2192 showMain()\n                     \u251c\u2500 identity_exists \u2192 showView(\"unlock\")\n                     \u2514\u2500 neither        \u2192 showView(\"onboarding\")\n</code></pre> <p>The bridge (<code>window.pywebview.api</code>) is not available at <code>DOMContentLoaded</code> \u2014 it becomes available asynchronously and fires <code>pywebviewready</code>. Both paths are handled.</p>"},{"location":"gui/overview/#views","title":"Views","text":"View id When shown <code>view-unlock</code> Identity exists, password not yet entered <code>view-onboarding</code> First launch, no identity <code>view-main</code> Identity loaded and Tor running/starting"},{"location":"gui/overview/#panels-inside-view-main","title":"Panels (inside <code>view-main</code>)","text":"Panel id Content <code>panel-welcome</code> Empty state placeholder <code>panel-messages</code> Private conversation with current contact <code>panel-group</code> Group chat with member list <code>panel-network</code> Network log with bandwidth display <code>panel-opsec</code> OpSec guide <code>panel-settings</code> Settings: alias, TTL, relay, panic button"},{"location":"gui/overview/#key-javascript-state","title":"Key JavaScript state","text":"Variable Type Purpose <code>API_PORT</code> <code>number</code> Port for all API calls <code>identity</code> <code>object \\| null</code> Current identity (public_key, alias, default_ttl, ...) <code>currentContact</code> <code>object \\| null</code> Open conversation contact <code>currentGroup</code> <code>object \\| null</code> Open group <code>eventSource</code> <code>EventSource \\| null</code> Active SSE connection <code>pendingFile</code> <code>object \\| null</code> File selected for attachment <code>networkLog</code> <code>array</code> Ring buffer of last 200 network log entries <code>defaultTtl</code> <code>number</code> Global default TTL in seconds <code>msgTtl</code> <code>number \\| null</code> Per-message TTL override (null = use default)"},{"location":"gui/overview/#api-helper","title":"API helper","text":"<pre><code>async function api(method, path, body = null) \u2192 any\n</code></pre> <p>All API calls go through this function. Handles: - JSON serialisation of request body - HTTP error handling (including FastAPI Pydantic validation error arrays) - Returns parsed JSON or null for HTTP 204</p>"},{"location":"gui/overview/#sse-connection","title":"SSE connection","text":"<p><code>connectSSE()</code> opens <code>EventSource</code> to <code>/api/events</code>. On disconnect, retries after 5 seconds. <code>handleEvent(event)</code> dispatches by <code>event.type</code> to update the relevant UI components.</p>"},{"location":"gui/overview/#message-ttl-picker","title":"Message TTL picker","text":"<p>The <code>\u23f1</code> button in the message input row opens a dropdown with TTL presets. State: - <code>defaultTtl</code> \u2014 loaded from <code>identity.default_ttl</code> at login - <code>msgTtl</code> \u2014 per-message override; null means use <code>defaultTtl</code>; reset to null after send</p>"},{"location":"gui/overview/#network-log","title":"Network log","text":"<p><code>appendNetLog(entry)</code> adds entries to the DOM only if <code>panel-network</code> is visible. <code>renderNetLog()</code> re-renders all entries when the filter changes or the panel is opened. <code>netLogAutoScroll</code> pauses auto-scroll when the user has scrolled up.</p>"},{"location":"gui/overview/#configpy","title":"config.py","text":"<pre><code>@dataclass\nclass Config:\n    api_port: int = 8080\n    title: str = \"Legion\"\n    width: int = 1100\n    height: int = 720\n    min_width: int = 800\n    min_height: int = 550\n    debug: bool = False\n</code></pre> <p>Simple dataclass, no external dependencies. Modified by CLI args in <code>main.py</code>.</p>"},{"location":"node/api/","title":"API server","text":"<p><code>api/server.py</code> contains the FastAPI application, all REST endpoints, the SSE event stream, the incoming message handler, and the <code>AppState</code> shared runtime object.</p>"},{"location":"node/api/#appstate","title":"AppState","text":"<pre><code>@dataclass\nclass AppState:\n    db: Database\n    delivery_queue: DeliveryQueue\n    identity: Identity | None = None\n    tor_onion: str = \"\"\n    tor_manager: object | None = None\n    node_port: int = 8765\n    _tor_starting: bool = False\n    tor_error: str = \"\"\n    _event_queues: list[asyncio.Queue] = field(default_factory=list)\n    _log_ring: deque = field(default_factory=lambda: deque(maxlen=200))\n</code></pre> <p>One instance is created in <code>main.py</code> and passed to the FastAPI app. Every request handler receives it via <code>Depends(get_state)</code>.</p>"},{"location":"node/api/#push_eventevent_type-data-none","title":"<code>push_event(event_type, data) \u2192 None</code>","text":"<p>Pushes a dict into every active SSE subscriber's queue (<code>maxsize=100</code> per subscriber). <code>QueueFull</code> is silently ignored \u2014 the GUI will refresh on the next user action.</p>"},{"location":"node/api/#push_network_loglevel-category-text-none","title":"<code>push_network_log(level, category, text) \u2192 None</code>","text":"<p>Appends a <code>network_log</code> entry to <code>_log_ring</code> (for SSE backfill on reconnect) and calls <code>push_event()</code>. Bandwidth events (<code>level=\"bw\"</code>) are streamed but not persisted in the ring \u2014 too frequent to be useful in history.</p>"},{"location":"node/api/#on_message_deliveredmessage_id-none","title":"<code>on_message_delivered(message_id) \u2192 None</code>","text":"<p>Async callback passed to <code>DeliveryQueue</code>. Pushes <code>delivery_status</code> SSE event and a delivery log entry.</p>"},{"location":"node/api/#create_appstate-fastapi","title":"<code>create_app(state) \u2192 FastAPI</code>","text":"<p>Factory function. Creates and configures the FastAPI application:</p> <ul> <li><code>docs_url=None, redoc_url=None</code> \u2014 API documentation endpoints disabled</li> <li>Adds <code>CORSMiddleware</code> with <code>allow_origin_regex=r\"https?://(localhost|127\\.0\\.0\\.1)(:\\d+)?\"</code>   \u2014 allows only localhost origins (pywebview's embedded HTTP server)</li> </ul>"},{"location":"node/api/#dependency-functions","title":"Dependency functions","text":""},{"location":"node/api/#get_state-appstate","title":"<code>get_state() \u2192 AppState</code>","text":"<p>Returns <code>app.state.app_state</code>. Injected into every endpoint.</p>"},{"location":"node/api/#require_identitys-tupleappstate-identity","title":"<code>require_identity(s) \u2192 tuple[AppState, Identity]</code>","text":"<p>Returns <code>(state, state.identity)</code> or raises HTTP 503 if identity is not loaded. Used by all endpoints that require an unlocked identity.</p>"},{"location":"node/api/#endpoints","title":"Endpoints","text":""},{"location":"node/api/#identity","title":"Identity","text":""},{"location":"node/api/#post-apiidentityunlock","title":"<code>POST /api/identity/unlock</code>","text":"<p>Unlocks an existing identity with a password:</p> <ol> <li>Returns current identity if already unlocked (idempotent)</li> <li>Loads identity row from DB</li> <li>Calls <code>decrypt_private_key(row[\"private_key\"], password)</code></li> <li>Raises HTTP 401 on wrong password</li> <li>Sets <code>state.identity</code></li> <li>Launches <code>_start_tor_background(state)</code> as an asyncio task</li> <li>Returns <code>{public_key, onion_address, alias, default_ttl}</code></li> </ol>"},{"location":"node/api/#post-apiidentitycreate-201","title":"<code>POST /api/identity/create</code> (201)","text":"<p>Creates a new identity:</p> <ol> <li>Raises HTTP 409 if identity already exists</li> <li>Calls <code>generate_identity(alias)</code></li> <li>Encrypts private key with <code>encrypt_private_key(private_key, password)</code></li> <li>Saves to DB</li> <li>Sets <code>state.identity</code>, launches Tor background task</li> <li>Returns <code>{public_key, onion_address, alias, default_ttl}</code></li> </ol>"},{"location":"node/api/#get-apiidentity","title":"<code>GET /api/identity</code>","text":"<p>Returns identity info including <code>default_ttl</code>.</p>"},{"location":"node/api/#patch-apiidentityalias","title":"<code>PATCH /api/identity/alias</code>","text":"<p>Updates alias in DB and replaces the in-memory <code>Identity</code> instance (frozen dataclass).</p>"},{"location":"node/api/#patch-apiidentitydefault_ttl","title":"<code>PATCH /api/identity/default_ttl</code>","text":"<p>Updates <code>default_ttl</code>. Clamps to <code>[3600, 2592000]</code> (1 hour \u2013 30 days).</p>"},{"location":"node/api/#get-apiidentitycard","title":"<code>GET /api/identity/card</code>","text":"<p>Returns a signed contact card via <code>protocol.build_contact_card()</code>.</p>"},{"location":"node/api/#delete-apiidentity-204-panic-button","title":"<code>DELETE /api/identity</code> (204) \u2014 Panic Button","text":"<ol> <li><code>db.panic_wipe()</code> \u2014 deletes all rows from all tables, then runs <code>VACUUM</code></li> <li>Sets <code>state.identity = None</code></li> </ol> <p>No authentication required \u2014 by design, so it can be triggered in an emergency even if the identity flow is in an unusual state.</p>"},{"location":"node/api/#contacts","title":"Contacts","text":""},{"location":"node/api/#get-apicontacts","title":"<code>GET /api/contacts</code>","text":"<p>Returns all contacts. If identity is loaded, enriches each with <code>unread_count</code> (count of unread incoming messages).</p>"},{"location":"node/api/#post-apicontacts-201","title":"<code>POST /api/contacts</code> (201)","text":"<p>Adds a contact from a contact card JSON:</p> <ol> <li>Calls <code>validate_contact_card(card)</code> \u2014 verifies Ed25519 signature</li> <li>Raises HTTP 422 on invalid card</li> <li>Saves contact with <code>alias = card.alias_hint</code> and <code>trusted_since = now</code></li> </ol>"},{"location":"node/api/#patch-apicontactspublic_keyalias","title":"<code>PATCH /api/contacts/{public_key}/alias</code>","text":"<p>Updates the local alias for a contact.</p>"},{"location":"node/api/#delete-apicontactspublic_key-204","title":"<code>DELETE /api/contacts/{public_key}</code> (204)","text":"<p>Deletes contact and all message history with that contact.</p>"},{"location":"node/api/#messages","title":"Messages","text":""},{"location":"node/api/#delete-apimessagesmessage_id-204","title":"<code>DELETE /api/messages/{message_id}</code> (204)","text":"<p>Cancels delivery retries for a queued message:</p> <ol> <li><code>db.cancel_queued(message_id)</code> \u2014 removes from delivery queue</li> <li><code>db.update_message_status(message_id, \"failed\")</code></li> </ol>"},{"location":"node/api/#post-apimessagespublic_keyread-204","title":"<code>POST /api/messages/{public_key}/read</code> (204)","text":"<p>Marks all incoming messages from <code>public_key</code> as read (<code>read_at = now</code>).</p>"},{"location":"node/api/#get-apimessagespublic_key","title":"<code>GET /api/messages/{public_key}</code>","text":"<p>Returns all messages in a conversation, decrypted on-the-fly by <code>_decrypt_message()</code>. Each message includes <code>text</code> (plaintext or null) and <code>file_data</code> (base64 or null).</p>"},{"location":"node/api/#post-apimessages-201","title":"<code>POST /api/messages</code> (201)","text":"<p>Sends a message:</p> <ol> <li>Resolves TTL: <code>req.ttl</code> \u2192 <code>identity.default_ttl</code> \u2192 <code>DEFAULT_TTL</code> (7 days)</li> <li>If <code>file_data</code>: decodes base64, calls <code>private.send_file()</code> (includes sanitization)</li> <li>Otherwise: calls <code>private.send()</code></li> <li>Calls <code>choose_destination()</code> to decide direct vs relay routing</li> <li>Enqueues in delivery queue</li> </ol>"},{"location":"node/api/#groups","title":"Groups","text":""},{"location":"node/api/#get-apigroups","title":"<code>GET /api/groups</code>","text":"<p>Returns all groups with <code>unread_count</code> (posts since <code>last_read_at</code>, excluding own posts).</p>"},{"location":"node/api/#post-apigroups-201","title":"<code>POST /api/groups</code> (201)","text":"<p>Creates a group via <code>groups.create_group()</code>.</p>"},{"location":"node/api/#delete-apigroupsid-204","title":"<code>DELETE /api/groups/{id}</code> (204)","text":"<p>Leaves or dissolves the group:</p> <ol> <li>Calls <code>groups.leave_group()</code> to build leave notifications</li> <li>Enqueues all notifications in delivery queue</li> <li>Calls <code>db.delete_group()</code> to remove locally</li> </ol>"},{"location":"node/api/#get-apigroupsidmembers","title":"<code>GET /api/groups/{id}/members</code>","text":"<p>Returns member list enriched with <code>alias</code> (priority: <code>contacts.alias</code> \u2192 <code>group_members.alias_hint</code>) and <code>is_admin</code> flag.</p>"},{"location":"node/api/#delete-apigroupsidmembersmember_key-204","title":"<code>DELETE /api/groups/{id}/members/{member_key}</code> (204)","text":"<p>Admin removes a member:</p> <ol> <li>Calls <code>groups.remove_member()</code> \u2014 generates new key, builds broadcasts</li> <li>Enqueues all broadcasts</li> </ol>"},{"location":"node/api/#post-apigroupsidinvite-201","title":"<code>POST /api/groups/{id}/invite</code> (201)","text":"<p>Invites a member:</p> <ol> <li>Calls <code>groups.invite_member()</code> \u2014 builds invite + update broadcasts</li> <li>Enqueues invite to the new member</li> <li>Enqueues <code>group_member_update(op=add)</code> to all existing members</li> </ol>"},{"location":"node/api/#post-apigroupsidread-204","title":"<code>POST /api/groups/{id}/read</code> (204)","text":"<p>Sets <code>last_read_at = now</code> for the group.</p>"},{"location":"node/api/#get-apigroupsidposts","title":"<code>GET /api/groups/{id}/posts</code>","text":"<p>Returns all posts decrypted on-the-fly. Enriches each with <code>author_alias</code> (priority: <code>contacts.alias</code> \u2192 <code>group_members.alias_hint</code>).</p>"},{"location":"node/api/#post-apigroupsidposts-201","title":"<code>POST /api/groups/{id}/posts</code> (201)","text":"<p>Posts to a group:</p> <ol> <li>Calls <code>groups.post()</code> \u2014 SecretBox encrypts, signs, saves locally</li> <li>Delivers to every member using <code>group_members.onion_address</code> (peer-to-peer routing)</li> </ol>"},{"location":"node/api/#status-and-tor","title":"Status and Tor","text":""},{"location":"node/api/#get-apistatus","title":"<code>GET /api/status</code>","text":"<p>Returns node state: <pre><code>{\n  \"identity_loaded\": true,\n  \"identity_exists\": true,\n  \"onion_address\": \"xxx.onion\",\n  \"tor_running\": true,\n  \"tor_starting\": false,\n  \"tor_error\": \"\",\n  \"relay_configured\": false\n}\n</code></pre></p>"},{"location":"node/api/#post-apitorretry-202","title":"<code>POST /api/tor/retry</code> (202)","text":"<p>Retries Tor startup if it previously failed.</p>"},{"location":"node/api/#sse-event-stream","title":"SSE event stream","text":""},{"location":"node/api/#get-apievents","title":"<code>GET /api/events</code>","text":"<p>Server-Sent Events stream. On connection:</p> <ol> <li>Backfill: sends all entries from <code>_log_ring</code> (up to 200 past <code>network_log</code> events)</li> <li>Live: streams events as they arrive</li> </ol> <p>Each event is <code>data: {json}\\n\\n</code>.</p> Event type Fields Trigger <code>message</code> <code>from, id</code> Incoming private message received <code>group_post</code> <code>group_id, from, id</code> Incoming group post received <code>group_invite</code> <code>group_id</code> Group invite accepted <code>group_member_update</code> <code>from, op, group_id, public_key, alias_hint, group_name, voluntary?, dissolved?</code> Roster change <code>group_key_update</code> <code>from, group_id</code> Group key rotated <code>delivery_status</code> <code>id, status</code> Message delivered <code>tor_ready</code> <code>onion_address</code> Hidden Service published <code>tor_status</code> <code>status, error?</code> Tor starting or failed <code>network_log</code> <code>level, category, text, ts</code> Network activity log entry"},{"location":"node/api/#_start_tor_backgroundstate-none","title":"<code>_start_tor_background(state) \u2192 None</code>","text":"<p>Async function launched as <code>asyncio.create_task()</code> after identity unlock.</p> <ol> <li>Guards against double-start (<code>_tor_starting</code> flag, <code>tm.is_running</code> check)</li> <li>Pushes <code>tor_status(starting)</code> SSE event</li> <li>Pushes <code>network_log</code> entry</li> <li>Calls <code>await tm.start(identity.private_key, hs_port=state.node_port)</code></li> <li>On success: updates <code>state.tor_onion</code>, pushes <code>tor_ready</code> event, attaches Stem event listener</li> <li>On failure: stores error in <code>state.tor_error</code>, pushes <code>tor_status(error)</code></li> </ol> <p>The Stem listener callback wraps <code>state.push_network_log</code> with <code>loop.call_soon_threadsafe()</code> to safely cross the thread boundary from Stem's event thread.</p>"},{"location":"node/api/#make_message_handlerstate-messagehandler","title":"<code>make_message_handler(state) \u2192 MessageHandler</code>","text":"<p>Returns the async handler passed to <code>NodeServer.start()</code>. Called for every valid incoming WebSocket message.</p> <p>Dispatch by <code>msg_type</code>:</p> Type Action <code>\"msg\"</code> Verify sender is in contacts \u2192 <code>private.receive()</code> \u2192 push <code>message</code> SSE <code>\"group_invite\"</code> Verify sender is in contacts \u2192 <code>groups.accept_invite()</code> \u2192 push <code>group_invite</code> SSE <code>\"group_post\"</code> Verify sender is a group member \u2192 <code>groups.receive_post()</code> \u2192 push <code>group_post</code> SSE <code>\"group_member_update\"</code> <code>groups.handle_member_update()</code> \u2192 push SSE; if admin receives voluntary leave \u2192 auto-rotate key via <code>groups.remove_member()</code> <code>\"group_key_update\"</code> <code>groups.handle_key_update()</code> \u2192 push <code>group_key_update</code> SSE <p>Unknown type or failed authorization: silent drop, no response. <code>state.identity is None</code> check at entry \u2014 drops all messages if the identity is not loaded.</p>"},{"location":"node/api/#run_appstate-port-none","title":"<code>run_app(state, port) \u2192 None</code>","text":"<p>Starts uvicorn bound to <code>127.0.0.1</code> only: <pre><code>uvicorn.Config(app, host=\"127.0.0.1\", port=port, log_level=\"warning\")\n</code></pre> Never binds to <code>0.0.0.0</code>. Uvicorn access logs are suppressed.</p>"},{"location":"node/api/#helper-functions","title":"Helper functions","text":""},{"location":"node/api/#_decrypt_messagerow-identity-dict","title":"<code>_decrypt_message(row, identity) \u2192 dict</code>","text":"<p>Decrypts a message row for API response:</p> <ol> <li>Determines peer key: if <code>from_key == our_key</code> then peer is <code>to_key</code>, and vice versa</li> <li>Calls <code>crypto.decrypt(our_private, peer_public, payload)</code></li> <li>Parses the JSON envelope</li> <li>Returns dict with <code>text</code> (str or null) and <code>file_data</code> (base64 str or null)</li> <li>Removes raw <code>payload</code> and <code>signature</code> from the response</li> </ol>"},{"location":"node/api/#_decrypt_postrow-group_key-dict","title":"<code>_decrypt_post(row, group_key) \u2192 dict</code>","text":"<p>Decrypts a group post row. Returns <code>text</code> field (or null on failure).</p>"},{"location":"node/api/#_group_safegroup-dict","title":"<code>_group_safe(group) \u2192 dict</code>","text":"<p>Removes <code>group_key</code> from the group dict before returning to the GUI. The raw 32-byte symmetric key is never sent to the frontend.</p>"},{"location":"node/api/#ttl-resolution-constants","title":"TTL resolution constants","text":"<pre><code>_TTL_MIN = 3_600      # 1 hour\n_TTL_MAX = 2_592_000  # 30 days\n</code></pre>"},{"location":"node/core/","title":"Core layer","text":"<p>The core layer provides the fundamental building blocks: cryptographic identity, all cryptographic operations, database access, and the network message protocol. No module in this layer initiates network connections or holds application state.</p>"},{"location":"node/core/#identitypy","title":"identity.py","text":"<p>Manages the user's Ed25519 keypair and its storage. All operations are pure functions or dataclass methods \u2014 no side effects beyond what the caller requests.</p>"},{"location":"node/core/#identity-dataclass-frozen","title":"<code>Identity</code> (dataclass, frozen)","text":"<pre><code>@dataclass(frozen=True)\nclass Identity:\n    public_key: bytes       # 32 bytes \u2014 Ed25519 verify key, also encodes the .onion address\n    private_key: bytes      # 32 bytes \u2014 Ed25519 seed, never leaves the device in plaintext\n    onion_address: str      # deterministic v3 .onion derived from public_key\n    alias: str              # local display name, not transmitted unless included in contact card\n</code></pre> <p>Frozen \u2014 once created the object is immutable. When the alias changes, a new <code>Identity</code> instance is constructed from the same keys.</p>"},{"location":"node/core/#functions","title":"Functions","text":""},{"location":"node/core/#generatealias-str-identity","title":"<code>generate(alias: str) \u2192 Identity</code>","text":"<p>Generates a fresh Ed25519 keypair using <code>nacl.signing.SigningKey.generate()</code> (CSPRNG). Derives the <code>.onion</code> address deterministically. Returns a new <code>Identity</code>. Called once, during <code>POST /api/identity/create</code>.</p>"},{"location":"node/core/#encrypt_private_keyprivate_key-bytes-password-str-bytes","title":"<code>encrypt_private_key(private_key: bytes, password: str) \u2192 bytes</code>","text":"<p>Protects the private key seed for storage on disk:</p> <ol> <li>Generates 16 random bytes of salt (<code>nacl.utils.random</code>)</li> <li>Derives a 32-byte encryption key from the password using Argon2id    (<code>OPSLIMIT_INTERACTIVE</code> / <code>MEMLIMIT_INTERACTIVE</code>)</li> <li>Encrypts the seed with XSalsa20-Poly1305 (SecretBox)</li> <li>Returns <code>salt (16 bytes) || ciphertext</code></li> </ol> <p>Raises <code>ValueError</code> if <code>private_key</code> is not 32 bytes.</p>"},{"location":"node/core/#decrypt_private_keyblob-bytes-password-str-bytes","title":"<code>decrypt_private_key(blob: bytes, password: str) \u2192 bytes</code>","text":"<p>Reverses <code>encrypt_private_key</code>. Splits <code>blob</code> into salt and ciphertext, re-derives the encryption key with Argon2id, decrypts with SecretBox. Raises <code>nacl.exceptions.CryptoError</code> on wrong password or corrupted blob. Called during <code>POST /api/identity/unlock</code>.</p>"},{"location":"node/core/#signing_keyidentity-identity-naclsigningsigningkey","title":"<code>signing_key(identity: Identity) \u2192 nacl.signing.SigningKey</code>","text":"<p>Reconstructs a libsodium <code>SigningKey</code> from the stored 32-byte seed. Used by <code>crypto.sign()</code> and wherever Tor needs the expanded private key.</p>"},{"location":"node/core/#derive_onion_addresspublic_key-bytes-str","title":"<code>derive_onion_address(public_key: bytes) \u2192 str</code>","text":"<p>Implements the Tor v3 Hidden Service address specification: <pre><code>address = base32(public_key || SHA3-256(\".onion checksum\" || public_key || \\x03)[0:2] || \\x03) + \".onion\"\n</code></pre> The address is a pure function of the public key \u2014 identical input always produces identical output. Called at identity generation and at Tor HS registration to verify the address matches.</p>"},{"location":"node/core/#cryptopy","title":"crypto.py","text":"<p>Thin wrappers around libsodium (PyNaCl) operations. No state, no I/O. Every function either succeeds or raises a <code>nacl.exceptions</code> error.</p>"},{"location":"node/core/#key-type-notes","title":"Key type notes","text":"<p>Legion identity keys are Ed25519. For Box encryption (asymmetric), they are converted to Curve25519 via libsodium's <code>crypto_sign_ed25519_sk_to_curve25519</code> and <code>crypto_sign_ed25519_pk_to_curve25519</code>. This conversion is a well-established technique used by OpenSSH and other protocols. The conversion is transparent \u2014 callers always pass Ed25519 keys.</p>"},{"location":"node/core/#functions_1","title":"Functions","text":""},{"location":"node/core/#signprivate_key-bytes-data-bytes-bytes","title":"<code>sign(private_key: bytes, data: bytes) \u2192 bytes</code>","text":"<p>Signs <code>data</code> with an Ed25519 private key seed. Returns 64-byte signature.</p>"},{"location":"node/core/#verifypublic_key-bytes-data-bytes-signature-bytes-none","title":"<code>verify(public_key: bytes, data: bytes, signature: bytes) \u2192 None</code>","text":"<p>Verifies an Ed25519 signature. Raises <code>nacl.exceptions.BadSignatureError</code> on failure. Callers treat any exception as invalid and drop the message silently.</p>"},{"location":"node/core/#encryptsender_private_key-bytes-recipient_public_key-bytes-plaintext-bytes-bytes","title":"<code>encrypt(sender_private_key: bytes, recipient_public_key: bytes, plaintext: bytes) \u2192 bytes</code>","text":"<p>Asymmetric authenticated encryption (Box):</p> <ol> <li>Converts both Ed25519 keys to Curve25519</li> <li>Performs X25519 key exchange</li> <li>Encrypts with XSalsa20-Poly1305 with a random nonce</li> <li>Returns <code>nonce (24 bytes) || ciphertext</code></li> </ol> <p>Used for: private messages, group invites, group roster updates, key rotation messages.</p>"},{"location":"node/core/#decryptrecipient_private_key-bytes-sender_public_key-bytes-ciphertext-bytes-bytes","title":"<code>decrypt(recipient_private_key: bytes, sender_public_key: bytes, ciphertext: bytes) \u2192 bytes</code>","text":"<p>Reverses <code>encrypt()</code>. Raises <code>nacl.exceptions.CryptoError</code> on authentication failure or wrong keys.</p>"},{"location":"node/core/#encrypt_groupgroup_key-bytes-plaintext-bytes-bytes","title":"<code>encrypt_group(group_key: bytes, plaintext: bytes) \u2192 bytes</code>","text":"<p>Symmetric authenticated encryption (SecretBox) with a 32-byte group key. Returns <code>nonce (24 bytes) || ciphertext</code>. Used for group posts.</p>"},{"location":"node/core/#decrypt_groupgroup_key-bytes-ciphertext-bytes-bytes","title":"<code>decrypt_group(group_key: bytes, ciphertext: bytes) \u2192 bytes</code>","text":"<p>Reverses <code>encrypt_group()</code>.</p>"},{"location":"node/core/#generate_group_key-bytes","title":"<code>generate_group_key() \u2192 bytes</code>","text":"<p>Returns 32 cryptographically random bytes. Used when creating a group or rotating the group key after member removal.</p>"},{"location":"node/core/#storagepy","title":"storage.py","text":"<p>Async SQLite wrapper using <code>aiosqlite</code>. All public methods are <code>async</code>. The schema is defined in <code>data/schema.sql</code>; migrations run automatically at startup.</p>"},{"location":"node/core/#database-class","title":"<code>Database</code> (class)","text":"<p>Single connection wrapper. All methods commit immediately after each write. WAL journal mode is enabled for better concurrent read performance.</p>"},{"location":"node/core/#opening-a-connection","title":"Opening a connection","text":"<pre><code>async with Database.open(path) as db:\n    ...\n</code></pre> <p><code>open()</code> is an async context manager that: 1. Opens the aiosqlite connection 2. Sets <code>row_factory = aiosqlite.Row</code> (rows as dict-like objects) 3. Enables <code>PRAGMA journal_mode=WAL</code> and <code>PRAGMA foreign_keys=ON</code> 4. Runs <code>schema.sql</code> via <code>executescript</code> 5. Applies any pending column migrations (idempotent <code>ALTER TABLE</code> calls)</p>"},{"location":"node/core/#schema-migrations","title":"Schema migrations","text":"<p>Handled by <code>_apply_schema()</code> with a list of <code>ALTER TABLE ... ADD COLUMN</code> statements. Each migration is wrapped in <code>try/except</code> \u2014 <code>duplicate column name</code> errors are silently ignored, making the migration idempotent across restarts and upgrades.</p>"},{"location":"node/core/#methods-by-table","title":"Methods by table","text":""},{"location":"node/core/#identity","title":"Identity","text":"Method Purpose <code>save_identity(public_key, private_key, ...)</code> INSERT OR REPLACE \u2014 called once at identity creation <code>load_identity() \u2192 dict \\| None</code> SELECT first row \u2014 called at startup and unlock <code>update_identity_alias(alias)</code> UPDATE alias field <code>update_identity_default_ttl(ttl)</code> UPDATE default_ttl (1h\u201330d, validated at API layer) <code>panic_wipe()</code> DELETE all rows from all tables, then VACUUM"},{"location":"node/core/#relay-config","title":"Relay config","text":"<p>Single-row table (<code>id=1</code> enforced by CHECK constraint). Stores the optional relay node configuration for the current user.</p> Method Purpose <code>save_relay_config(onion_address, public_key, enabled)</code> INSERT OR REPLACE with id=1 <code>load_relay_config() \u2192 dict \\| None</code> SELECT WHERE id=1 <code>delete_relay_config()</code> DELETE WHERE id=1 <p><code>panic_wipe()</code> runs <code>VACUUM</code> after deletion. SQLite <code>VACUUM</code> rewrites the entire database file from scratch \u2014 freed pages (deleted rows) are not present in the new file.</p>"},{"location":"node/core/#contacts","title":"Contacts","text":"Method Purpose <code>save_contact(public_key, onion_address, alias, trusted_since)</code> INSERT OR REPLACE <code>get_contacts() \u2192 list[dict]</code> SELECT all <code>get_contact(public_key) \u2192 dict \\| None</code> SELECT by PK <code>is_contact(public_key) \u2192 bool</code> EXISTS check \u2014 used in message filtering <code>update_contact_alias(public_key, alias)</code> UPDATE <code>delete_contact(public_key)</code> DELETE <code>delete_messages_with_peer(peer_key)</code> DELETE FROM messages WHERE from_key=? OR to_key=?"},{"location":"node/core/#messages","title":"Messages","text":"Method Purpose <code>save_message(id, from_key, to_key, payload, signature, timestamp, expires_at, status, file_name, mime_type)</code> INSERT OR IGNORE \u2014 deduplicates by SHA256 id <code>get_message_by_id(id) \u2192 dict \\| None</code> SELECT by PK <code>get_messages(peer_key, our_key) \u2192 list[dict]</code> SELECT conversation ordered by timestamp <code>update_message_status(id, status)</code> UPDATE status field <code>mark_conversation_read(peer_key, our_key)</code> UPDATE read_at = now WHERE read_at IS NULL <code>get_unread_counts(our_key) \u2192 dict</code> {sender_key: count} for unread incoming <code>cancel_queued(message_id)</code> DELETE FROM delivery_queue WHERE message_id=? <code>delete_expired_messages(now)</code> DELETE WHERE expires_at &lt; now"},{"location":"node/core/#groups","title":"Groups","text":"Method Purpose <code>save_group(id, name, group_key, admin_key, is_admin, created_at)</code> INSERT OR REPLACE \u2014 explicit column names to handle added columns <code>get_groups() \u2192 list[dict]</code> SELECT all <code>get_group(id) \u2192 dict \\| None</code> SELECT by PK <code>delete_group(id)</code> DELETE from groups + group_members + group_posts <code>update_group_key(group_id, new_key)</code> UPDATE after key rotation <code>mark_group_read(group_id, now)</code> UPDATE last_read_at <code>get_group_unread_count(group_id, our_key) \u2192 int</code> COUNT posts after last_read_at, excluding own posts"},{"location":"node/core/#group-members","title":"Group members","text":"Method Purpose <code>save_group_member(group_id, public_key, added_at, onion_address, alias_hint)</code> INSERT OR REPLACE <code>get_group_members(group_id) \u2192 list[dict]</code> SELECT all for group <code>is_group_member(group_id, public_key) \u2192 bool</code> EXISTS check \u2014 used in message filtering <code>delete_group_member(group_id, public_key)</code> DELETE <p><code>onion_address</code> is stored per-member so posts can be routed peer-to-peer without requiring a contact record. <code>alias_hint</code> is the display name suggested at invite time.</p>"},{"location":"node/core/#group-posts","title":"Group posts","text":"Method Purpose <code>save_group_post(id, group_id, author_key, payload, signature, timestamp, expires_at)</code> INSERT OR IGNORE <code>get_group_posts(group_id) \u2192 list[dict]</code> SELECT ordered by timestamp <code>delete_expired_group_posts(now) \u2192 int</code> DELETE WHERE expires_at &lt; now"},{"location":"node/core/#cleanup","title":"Cleanup","text":"Method Purpose <code>delete_expired_messages(now) \u2192 int</code> DELETE messages WHERE expires_at &lt; now; returns row count <code>delete_expired_group_posts(now) \u2192 int</code> DELETE group_posts WHERE expires_at &lt; now; returns row count <p>These are not called on a schedule in the current implementation \u2014 they are available for future use by a maintenance task.</p>"},{"location":"node/core/#delivery-queue","title":"Delivery queue","text":"Method Purpose <code>enqueue(id, message_id, destination_key, destination_onion, next_retry_at, message_json, via_relay)</code> INSERT OR IGNORE <code>get_due(now) \u2192 list[dict]</code> SELECT WHERE next_retry_at &lt;= now <code>update_retry(id, next_retry_at)</code> UPDATE next_retry_at + increment retry_count <code>dequeue(id)</code> DELETE on successful delivery <p><code>message_json</code> stores the complete serialised protocol message (JSON string). This avoids reconstructing messages from the <code>messages</code> table, which would not work for <code>group_invite</code>, <code>group_key_update</code> and other types that are not stored there.</p>"},{"location":"node/core/#protocolpy","title":"protocol.py","text":"<p>Defines the wire format for all network messages, builds them, parses them, and validates them. No I/O, no state.</p>"},{"location":"node/core/#message-types-constants","title":"Message types (constants)","text":"Constant Wire value Used for <code>MSG_PRIVATE</code> <code>\"msg\"</code> Private text/file messages <code>MSG_GROUP_POST</code> <code>\"group_post\"</code> Group chat posts <code>MSG_GROUP_INVITE</code> <code>\"group_invite\"</code> Inviting a new group member <code>MSG_GROUP_MEMBER_UPDATE</code> <code>\"group_member_update\"</code> Roster changes (add/remove/leave) <code>MSG_GROUP_KEY_UPDATE</code> <code>\"group_key_update\"</code> Key rotation after member removal <code>MSG_DELIVERY_ACK</code> <code>\"delivery_ack\"</code> Delivery acknowledgement (reserved) <code>MSG_CONTACT_CARD</code> <code>\"contact_card\"</code> Contact card exchange (reserved)"},{"location":"node/core/#wire-format","title":"Wire format","text":"<p>Every message sent over the network is a JSON object:</p> <pre><code>{\n  \"v\": 1,\n  \"type\": \"msg\",\n  \"id\": \"&lt;SHA256 of payload, hex&gt;\",\n  \"from\": \"&lt;sender public key, hex&gt;\",\n  \"to\": \"&lt;recipient public key or group id, hex&gt;\",\n  \"payload\": \"&lt;base64-encoded ciphertext&gt;\",\n  \"signature\": \"&lt;base64-encoded Ed25519 signature&gt;\",\n  \"timestamp\": 1714000000,\n  \"ttl\": 604800\n}\n</code></pre> <p>The signature covers: <code>type|id|from|to|timestamp</code> (pipe-separated, UTF-8 encoded). This binds the signature to the message identity and prevents cross-type replay.</p>"},{"location":"node/core/#functions_2","title":"Functions","text":""},{"location":"node/core/#build_messagetype-from_key-to_key-payload-private_key-ttl-dict","title":"<code>build_message(type, from_key, to_key, payload, private_key, ttl) \u2192 dict</code>","text":"<p>Constructs and signs a protocol message:</p> <ol> <li>Computes <code>id = SHA256(payload).hexdigest()</code></li> <li>Sets <code>timestamp = int(time.time())</code></li> <li>Signs <code>type|id|from_hex|to_hex|timestamp</code> with the sender's private key</li> <li>Returns the complete message dict</li> </ol> <p><code>payload</code> is expected to be already encrypted by the caller.</p>"},{"location":"node/core/#parse_messageraw-str-bytes-dict","title":"<code>parse_message(raw: str | bytes) \u2192 dict</code>","text":"<p>Parses JSON and checks required fields are present. Raises <code>MalformedMessage</code> on invalid JSON or missing fields. Does not verify the signature \u2014 that is done by <code>validate_message()</code>.</p>"},{"location":"node/core/#validate_messagemsg-dict-now-int-none-none","title":"<code>validate_message(msg: dict, now: int | None) \u2192 None</code>","text":"<p>Full validation pipeline:</p> <ol> <li>Version check (<code>v == 1</code>)</li> <li>Type must be in <code>_VALID_TYPES</code></li> <li>Payload must be valid base64</li> <li><code>SHA256(payload) == id</code></li> <li><code>timestamp</code> must not be more than 300 seconds in the future (clock skew guard)</li> <li><code>now - timestamp</code> must not exceed <code>ttl</code> (expiry check)</li> <li><code>from</code> must be a valid 32-byte hex public key</li> <li>Ed25519 signature must verify against the signed data</li> </ol> <p>Any failure raises a <code>ProtocolError</code> subclass. The caller is responsible for silently dropping the message without sending any response.</p>"},{"location":"node/core/#contact-card-functions","title":"Contact card functions","text":""},{"location":"node/core/#build_contact_cardpublic_key-onion_address-private_key-alias_hint-dict","title":"<code>build_contact_card(public_key, onion_address, private_key, alias_hint) \u2192 dict</code>","text":"<p>Builds a signed JSON contact card for sharing with potential contacts. Signs the entire card (without the <code>signature</code> field) with the owner's private key.</p>"},{"location":"node/core/#validate_contact_cardcard-dict-none","title":"<code>validate_contact_card(card: dict) \u2192 None</code>","text":"<p>Verifies the Ed25519 signature on a received contact card. Raises <code>ProtocolError</code> on failure. Called before adding any contact.</p>"},{"location":"node/core/#exceptions","title":"Exceptions","text":"Exception Raised when <code>MalformedMessage</code> Invalid JSON, missing fields, bad encoding <code>InvalidSignature</code> Ed25519 verification failed <code>ExpiredMessage</code> Age &gt; TTL or timestamp too far in future <code>InvalidMessageId</code> SHA256(payload) \u2260 declared id"},{"location":"node/messaging/","title":"Messaging layer","text":"<p>The messaging layer implements the application-level message semantics on top of the core cryptography and storage. It handles payload construction, encryption, sanitization, group lifecycle management, and the delivery queue.</p>"},{"location":"node/messaging/#privatepy","title":"private.py","text":"<p>Private message exchange between two users. All payloads use a JSON envelope inside the encrypted blob so the same message type can carry text or files.</p>"},{"location":"node/messaging/#payload-envelope","title":"Payload envelope","text":"<p>The plaintext inside every private message ciphertext is a JSON object:</p> <pre><code>{ \"t\": \"hello world\" }           // text message\n{ \"f\": \"&lt;base64&gt;\", \"n\": \"photo.jpg\", \"m\": \"image/jpeg\" }  // file\n</code></pre> <p>This envelope is inside the Box ciphertext \u2014 the outer protocol layer only sees an opaque encrypted blob.</p>"},{"location":"node/messaging/#functions","title":"Functions","text":""},{"location":"node/messaging/#senddb-identity-recipient_public_key-plaintext-ttl-dict","title":"<code>send(db, identity, recipient_public_key, plaintext, ttl) \u2192 dict</code>","text":"<p>Sends a text message:</p> <ol> <li>Encodes <code>{\"t\": plaintext}</code> to bytes</li> <li>Encrypts with <code>crypto.encrypt(identity.private_key, recipient_public_key, payload)</code></li> <li>Calls <code>protocol.build_message()</code> to sign and create the protocol dict</li> <li>Saves to <code>messages</code> table with <code>status=\"queued\"</code></li> <li>Returns the message dict (caller enqueues it for delivery)</li> </ol>"},{"location":"node/messaging/#send_filedb-identity-recipient_public_key-file_data-file_name-mime_type-ttl-dict","title":"<code>send_file(db, identity, recipient_public_key, file_data, file_name, mime_type, ttl) \u2192 dict</code>","text":"<p>Sends a file:</p> <ol> <li>Calls <code>files.prepare_outgoing_async()</code> \u2014 sanitizes and validates the file</li> <li>Encodes <code>{\"f\": base64(sanitized), \"n\": file_name, \"m\": mime_type}</code></li> <li>Encrypts, builds message, saves with <code>file_name</code> and <code>mime_type</code> columns populated</li> <li>Returns the message dict</li> </ol> <p>Raises <code>FileError</code> if the file fails sanitization.</p>"},{"location":"node/messaging/#receivedb-identity-msg-dict","title":"<code>receive(db, identity, msg) \u2192 dict</code>","text":"<p>Processes an incoming message after protocol validation:</p> <ol> <li>Verifies <code>msg[\"to\"] == identity.public_key.hex()</code></li> <li>Decrypts: <code>crypto.decrypt(identity.private_key, sender_public_key, ciphertext)</code></li> <li>Decodes the JSON envelope</li> <li>If it's a file (<code>\"f\"</code> key): runs <code>files.sanitize_incoming_async()</code> \u2014 defense-in-depth</li> <li>Saves to <code>messages</code> table with <code>status=\"delivered\"</code></li> <li>Explicitly calls <code>update_message_status()</code> to ensure delivery status is set even if    the message was already in the DB (e.g. from a sent copy \u2014 <code>INSERT OR IGNORE</code> would    leave the old status)</li> <li>Returns the decoded payload dict</li> </ol>"},{"location":"node/messaging/#get_conversationdb-our_key-peer_key-listdict","title":"<code>get_conversation(db, our_key, peer_key) \u2192 list[dict]</code>","text":"<p>Returns all messages between two users ordered by timestamp. The result contains raw database rows with encrypted <code>payload</code> \u2014 decryption is done on-the-fly in the API layer.</p>"},{"location":"node/messaging/#_decode_payloadraw-bytes-dict","title":"<code>_decode_payload(raw: bytes) \u2192 dict</code>","text":"<p>Decodes the JSON envelope. Falls back to raw text in two cases:</p> <ol> <li><code>raw</code> is not valid JSON (<code>json.JSONDecodeError</code> or <code>UnicodeDecodeError</code>)</li> <li><code>raw</code> is valid JSON but the result is not a dict with a <code>\"t\"</code> or <code>\"f\"</code> key</li> </ol> <p>In both cases returns <code>{\"t\": raw.decode(errors=\"replace\")}</code> for compatibility with any pre-envelope messages.</p>"},{"location":"node/messaging/#groupspy","title":"groups.py","text":"<p>Full group lifecycle: creation, invitations, posting, member management, and dissolution. Groups use symmetric SecretBox encryption; all roster management messages use asymmetric Box encryption (one copy per recipient).</p>"},{"location":"node/messaging/#data-model","title":"Data model","text":"<p>A group is stored as: - One row in <code>groups</code>: <code>id, name, group_key (32 bytes), admin_key, is_admin, created_at</code> - One row per member in <code>group_members</code>: <code>group_id, public_key, onion_address, alias_hint, added_at</code></p> <p>The <code>group_key</code> is the shared SecretBox key. <code>admin_key</code> is the public key of the creator. <code>is_admin</code> is a local flag (True on the admin's node, False on members' nodes).</p>"},{"location":"node/messaging/#group-id","title":"Group ID","text":"<pre><code>group_id = SHA256(admin_public_key || name_bytes || timestamp_bytes).hexdigest()\n</code></pre> <p>64-character hex string. Deterministic from the admin's key, group name and creation time. Used as the <code>to</code> field in <code>group_post</code> messages.</p>"},{"location":"node/messaging/#functions_1","title":"Functions","text":""},{"location":"node/messaging/#create_groupdb-identity-name-dict","title":"<code>create_group(db, identity, name) \u2192 dict</code>","text":"<p>Creates a new group:</p> <ol> <li>Generates a random 32-byte <code>group_key</code></li> <li>Computes <code>group_id</code></li> <li>Saves group to DB</li> <li>Saves admin as first member with <code>onion_address=identity.onion_address</code> and    <code>alias_hint=identity.alias</code> \u2014 the admin's onion must be in the roster for new    members to be able to post back to them</li> </ol>"},{"location":"node/messaging/#invite_memberdb-identity-group_id-member_public_key-member_onion-ttl-tupledict-list","title":"<code>invite_member(db, identity, group_id, member_public_key, member_onion, ttl) \u2192 tuple[dict, list]</code>","text":"<p>Builds the invite and broadcasts the roster update to existing members:</p> <ol> <li>Fetches current member list with <code>onion_address</code> and <code>alias_hint</code></li> <li>Resolves alias hints (prefer <code>contacts.alias</code>, fall back to stored <code>alias_hint</code>)</li> <li>Builds invite JSON payload:    <pre><code>{ \"group_id\": \"...\", \"name\": \"...\", \"key\": \"&lt;base64&gt;\", \"members\": [...] }\n</code></pre></li> <li>Box-encrypts the entire payload for the invitee \u2014 group metadata is not visible on wire</li> <li>Builds a <code>group_member_update(op=add)</code> message for each existing member (Box-encrypted    per recipient) containing the new member's key, onion and alias_hint</li> <li>Saves new member to admin's <code>group_members</code> with their <code>onion_address</code> and <code>alias_hint</code></li> <li>Returns <code>(invite_msg, [(update_msg, destination_onion), ...])</code></li> </ol> <p>Why Box-encrypt the invite payload? The original approach stored group_id and name in plaintext inside the payload. The current implementation Box-encrypts the entire payload \u2014 only the invitee can read group metadata.</p>"},{"location":"node/messaging/#accept_invitedb-identity-msg-dict","title":"<code>accept_invite(db, identity, msg) \u2192 dict</code>","text":"<p>Processes a received <code>group_invite</code> after protocol validation:</p> <ol> <li>Box-decrypts the payload (sender is admin)</li> <li>Extracts group_id, name, group_key, member roster</li> <li>Saves group to DB</li> <li>Saves all roster members with their <code>onion_address</code> and <code>alias_hint</code></li> <li>Adds self if not already present (avoids overwriting admin's stored onion for us)</li> <li>Returns group record (without group_key \u2014 not exposed to API callers)</li> </ol>"},{"location":"node/messaging/#remove_memberdb-identity-group_id-member_public_key-ttl-tuplebytes-list","title":"<code>remove_member(db, identity, group_id, member_public_key, ttl) \u2192 tuple[bytes, list]</code>","text":"<p>Removes a member and rotates the group key:</p> <ol> <li>Saves removed member's onion address before deleting (needed for notification)</li> <li>Deletes member from <code>group_members</code></li> <li>Generates new <code>group_key</code> with <code>crypto.generate_group_key()</code></li> <li>Saves new key to DB</li> <li>Builds notification to the removed member: <code>group_member_update(op=remove, public_key=their_key)</code></li> <li>For each remaining member builds:</li> <li><code>group_key_update</code>: new key Box-encrypted for them, TTL=30 days</li> <li><code>group_member_update(op=remove)</code>: notify that removed member is gone</li> <li>Returns <code>(new_key, broadcasts)</code></li> </ol> <p>The removed member receives their notification and calls <code>delete_group</code> on their node. Remaining members receive the new key and update their roster.</p>"},{"location":"node/messaging/#leave_groupdb-identity-group_id-ttl-list","title":"<code>leave_group(db, identity, group_id, ttl) \u2192 list</code>","text":"<p>Called before <code>DELETE /api/groups/{id}</code>. Two different paths:</p> <p>Admin dissolving the group: Sends each member a <code>group_member_update(op=remove, public_key=their_own_key, dissolved=True)</code>. Each member receives a message where the public_key is their own \u2192 <code>removed_self</code> \u2192 <code>delete_group</code>. The <code>dissolved=True</code> flag lets the GUI show \"Group was dissolved by the admin\" instead of \"You were removed\".</p> <p>Regular member leaving: Sends each member <code>group_member_update(op=remove, public_key=self, voluntary=True)</code>. The member signs their own departure \u2014 only they can claim to be leaving. Upon receiving this, the admin's node automatically calls <code>remove_member()</code> to rotate the key.</p>"},{"location":"node/messaging/#handle_member_updatedb-identity-msg-dict-none","title":"<code>handle_member_update(db, identity, msg) \u2192 dict | None</code>","text":"<p>Processes an incoming <code>group_member_update</code>:</p> <ol> <li>Box-decrypts payload</li> <li>Looks up the group \u2014 ignores if unknown</li> <li>Authorization check:</li> <li>Accept from admin for any operation</li> <li>Also accept <code>op=remove</code> where <code>public_key==msg[\"from\"]</code> and <code>voluntary=True</code>      (self-removal signed by the departing member)</li> <li>For <code>op=add</code>: saves new member to <code>group_members</code></li> <li>For <code>op=remove</code> where <code>public_key==own_key</code>: calls <code>delete_group</code> (we were removed)</li> <li>For <code>op=remove</code> other member: calls <code>delete_group_member</code></li> <li>Returns info dict for SSE event, including <code>voluntary</code> and <code>dissolved</code> flags</li> </ol>"},{"location":"node/messaging/#handle_key_updatedb-identity-msg-str-none","title":"<code>handle_key_update(db, identity, msg) \u2192 str | None</code>","text":"<p>Processes an incoming <code>group_key_update</code>:</p> <ol> <li>Box-decrypts payload</li> <li>Verifies sender is the group admin</li> <li>Validates new key is exactly 32 bytes</li> <li>Calls <code>update_group_key()</code> to replace the key in DB</li> <li>Returns <code>group_id</code> for SSE event, or None if ignored</li> </ol>"},{"location":"node/messaging/#postdb-identity-group_id-plaintext-ttl-dict","title":"<code>post(db, identity, group_id, plaintext, ttl) \u2192 dict</code>","text":"<p>Sends a group post:</p> <ol> <li>Encrypts <code>plaintext.encode()</code> with <code>crypto.encrypt_group(group_key, plaintext)</code></li> <li>Builds protocol message with <code>to = bytes.fromhex(group_id)</code></li> <li>Saves to <code>group_posts</code></li> <li>Returns message dict \u2014 caller delivers to all members via <code>group_members.onion_address</code></li> </ol> <p>Key difference from private messages: posts use SecretBox (symmetric key shared by all members), not Box (asymmetric per-recipient). One ciphertext is sent to all members.</p>"},{"location":"node/messaging/#receive_postdb-identity-group_id-msg-str","title":"<code>receive_post(db, identity, group_id, msg) \u2192 str</code>","text":"<p>Decrypts and stores an incoming post. Returns plaintext. Raises <code>LookupError</code> if the group is unknown, <code>CryptoError</code> if decryption fails (e.g. after receiving a <code>group_key_update</code> out of order).</p>"},{"location":"node/messaging/#filespy","title":"files.py","text":"<p>File validation and sanitization. CPU-bound Pillow operations run in an executor.</p>"},{"location":"node/messaging/#constants","title":"Constants","text":"Constant Value <code>MAX_FILE_SIZE</code> 5 MB Supported MIME types <code>image/jpeg</code>, <code>image/png</code>, <code>image/webp</code> Blocked MIME types <code>image/svg+xml</code>, <code>text/html</code>, <code>application/xhtml+xml</code> <p>SVG is explicitly blocked because it can contain JavaScript.</p>"},{"location":"node/messaging/#functions_2","title":"Functions","text":""},{"location":"node/messaging/#prepare_outgoingdata-file_name-mime_type-bytes","title":"<code>prepare_outgoing(data, file_name, mime_type) \u2192 bytes</code>","text":"<p>Called on the sender side before encryption:</p> <ol> <li><code>_validate_size(data)</code> \u2014 rejects files over 5 MB</li> <li><code>_validate_file_name(file_name)</code> \u2014 rejects names with path separators, null bytes,    hidden files (starting with <code>.</code>)</li> <li>Rejects blocked MIME types</li> <li>For image types: calls <code>_sanitize_image()</code></li> <li>For other types: passes through (size and name already validated)</li> </ol>"},{"location":"node/messaging/#sanitize_incomingdata-mime_type-bytes","title":"<code>sanitize_incoming(data, mime_type) \u2192 bytes</code>","text":"<p>Called on the receiver side after decryption \u2014 defense-in-depth: Even if the sender skipped sanitization or used a modified client, the receiver sanitizes before saving. Re-validates size and re-sanitizes images.</p>"},{"location":"node/messaging/#prepare_outgoing_async-sanitize_incoming_async","title":"<code>prepare_outgoing_async</code> / <code>sanitize_incoming_async</code>","text":"<p>Async wrappers using <code>asyncio.get_running_loop().run_in_executor(None, ...)</code>. Pillow image operations are CPU-bound and must not block the event loop.</p>"},{"location":"node/messaging/#_sanitize_imagedata-mime_type-bytes","title":"<code>_sanitize_image(data, mime_type) \u2192 bytes</code>","text":"<p>Core sanitization:</p> <ol> <li>Checks magic bytes against declared MIME type (e.g. <code>\\xff\\xd8\\xff</code> for JPEG)</li> <li><code>Image.open(io.BytesIO(data))</code> + <code>img.load()</code> \u2014 full decode, catches truncated images</li> <li>Converts JPEG to RGB or L mode if needed</li> <li>Saves to a new <code>BytesIO</code> buffer with no <code>exif</code>, <code>icc_profile</code> or metadata kwargs</li> <li>Returns the new bytes</li> </ol> <p>Re-encoding from scratch is more thorough than clearing specific metadata fields \u2014 it is impossible to smuggle data in format-specific metadata structures that Pillow does not understand.</p>"},{"location":"node/messaging/#fileerror-exception","title":"<code>FileError</code> (exception)","text":"<p>Raised on size violation, invalid filename, blocked type, magic bytes mismatch, or image decode failure.</p>"},{"location":"node/messaging/#deliverypy","title":"delivery.py","text":"<p>Persistent background queue that retries message delivery every 10 seconds until the message is delivered or the user cancels it. State is stored in SQLite so the queue survives application restarts.</p>"},{"location":"node/messaging/#deliveryqueue-class","title":"<code>DeliveryQueue</code> (class)","text":"<pre><code>DeliveryQueue(db: Database, sender: Sender, on_delivered: OnDelivered | None)\n</code></pre> <p><code>sender</code> is <code>async (msg_dict: dict, onion: str) \u2192 None</code> \u2014 the actual network send function. <code>on_delivered</code> is <code>async (message_id: str) \u2192 None</code> \u2014 called after successful delivery (used by <code>AppState.on_message_delivered</code> to push <code>delivery_status</code> SSE event).</p>"},{"location":"node/messaging/#enqueuemsg-destination_onion-via_relay-none","title":"<code>enqueue(msg, destination_onion, via_relay) \u2192 None</code>","text":"<p>Adds a message to the delivery queue for immediate first attempt:</p> <ol> <li>Computes deterministic entry id: <code>SHA256(message_id:destination_key)</code></li> <li>Sets <code>next_retry_at = int(time.time())</code> (due immediately)</li> <li><code>INSERT OR IGNORE</code> \u2014 duplicate entries for the same message+destination are silently dropped</li> <li>Calls <code>self._wake.set()</code> \u2014 wakes the delivery loop immediately rather than waiting 10 seconds</li> </ol>"},{"location":"node/messaging/#process_duenow-tupleint-int","title":"<code>process_due(now) \u2192 tuple[int, int]</code>","text":"<p>Fetches all due entries (<code>next_retry_at &lt;= now</code>) and calls <code>_try_deliver()</code> for each. Returns <code>(sent, failed)</code> counts.</p>"},{"location":"node/messaging/#start-stop","title":"<code>start() / stop()</code>","text":"<p>Starts/stops the background <code>asyncio.Task</code> running <code>_loop()</code>.</p>"},{"location":"node/messaging/#_loop-none","title":"<code>_loop() \u2192 None</code>","text":"<p>Main delivery loop: <pre><code>while running:\n    await process_due()\n    self._wake.clear()\n    try:\n        await asyncio.wait_for(self._wake.wait(), timeout=10)\n    except asyncio.TimeoutError:\n        pass\n</code></pre> Sleeps 10 seconds OR wakes immediately if <code>_wake</code> is set by <code>enqueue()</code>.</p>"},{"location":"node/messaging/#_try_deliverentry-now-bool-none","title":"<code>_try_deliver(entry, now) \u2192 bool | None</code>","text":"<p>Attempts delivery of one queue entry:</p> <ol> <li>Loads <code>msg_dict</code> from <code>entry[\"message_json\"]</code></li> <li>Checks if the message has expired (<code>now - timestamp &gt; ttl</code>) \u2014 removes orphans silently</li> <li>Calls <code>await self._sender(msg_dict, entry[\"destination_onion\"])</code></li> <li>On success: dequeues, updates message status to <code>\"delivered\"</code>, calls <code>on_delivered</code></li> <li>On failure: updates <code>next_retry_at = now + 10</code> and increments <code>retry_count</code></li> </ol> <p>Returns <code>True</code> (sent), <code>False</code> (failed, retry scheduled), or <code>None</code> (orphan removed).</p> <p>There is no maximum retry count \u2014 the queue retries indefinitely until the message is delivered or the user cancels via <code>DELETE /api/messages/{id}</code>.</p>"},{"location":"node/messaging/#type-aliases","title":"Type aliases","text":"<pre><code>Sender      = Callable[[dict, str], Awaitable[None]]   # (msg_dict, onion) \u2192 None\nOnDelivered = Callable[[str], Awaitable[None]]          # (message_id) \u2192 None\n</code></pre>"},{"location":"node/network/","title":"Network layer","text":"<p>The network layer handles all communication between nodes: managing the Tor process, accepting incoming messages, sending outgoing messages through Tor SOCKS5, and selecting whether to send directly or via a relay.</p>"},{"location":"node/network/#torpy","title":"tor.py","text":"<p>Manages the Tor subprocess lifecycle and the v3 Hidden Service registration. All blocking Stem calls are run in an executor to avoid blocking the asyncio event loop.</p>"},{"location":"node/network/#tormanager-class","title":"<code>TorManager</code> (class)","text":"<pre><code>TorManager(data_dir: Path, socks_port: int = 9050, control_port: int = 9051)\n</code></pre>"},{"location":"node/network/#startprivate_key-bytes-hs_port-int-str","title":"<code>start(private_key: bytes, hs_port: int) \u2192 str</code>","text":"<p>Full startup sequence:</p> <ol> <li>Creates data directory if needed</li> <li>Runs <code>_launch()</code> in executor with <code>asyncio.wait_for(timeout=120s)</code></li> <li>Connects to Tor control port via Stem</li> <li>Authenticates to the controller</li> <li>Calls <code>_create_hidden_service()</code> in executor</li> <li>Returns the <code>.onion</code> address</li> </ol> <p>If any step fails, calls <code>stop()</code> to clean up before raising <code>TorError</code>.</p>"},{"location":"node/network/#stop-none","title":"<code>stop() \u2192 None</code>","text":"<p>Closes the Stem controller connection, then terminates the Tor subprocess. If <code>terminate()</code> doesn't work within 5 seconds, escalates to <code>kill()</code>.</p>"},{"location":"node/network/#attach_log_listenercallback-logcallback-none","title":"<code>attach_log_listener(callback: LogCallback) \u2192 None</code>","text":"<p>Registers Stem event listeners after <code>start()</code> succeeds. <code>callback(level, category, text)</code> is called from Stem's event thread. The caller must wrap it with <code>loop.call_soon_threadsafe()</code> to safely push events into the asyncio event loop (done in <code>api/server.py:_start_tor_background</code>).</p> <p>Registered event types:</p> Event Handler Purpose <code>CIRC</code> <code>_on_circ</code> HS circuit establishment/failure <code>BW</code> <code>_on_bw</code> Bandwidth stats (bytes/s read:written) <code>STATUS_CLIENT</code> <code>_on_status</code> Bootstrap state changes <code>WARN</code>, <code>ERR</code> <code>_on_warn</code> Tor warning and error messages"},{"location":"node/network/#properties","title":"Properties","text":"Property Type Description <code>onion_address</code> <code>str</code> <code>.onion</code> address \u2014 raises <code>TorError</code> if not running <code>socks_port</code> <code>int</code> SOCKS5 port for outgoing connections <code>is_running</code> <code>bool</code> True if both process and controller are active"},{"location":"node/network/#internal-helpers","title":"Internal helpers","text":""},{"location":"node/network/#_launch-process","title":"<code>_launch() \u2192 process</code>","text":"<p>Called in an executor. Runs <code>stem.process.launch_tor_with_config()</code> without a <code>timeout=</code> parameter \u2014 Stem's timeout uses <code>signal.SIGALRM</code> which only works from the main thread. The 120-second timeout is handled by <code>asyncio.wait_for</code> in <code>start()</code>.</p> <p>Tor config: <pre><code>{\n    \"SocksPort\": str(socks_port),\n    \"ControlPort\": str(control_port),\n    \"DataDirectory\": str(data_dir),\n    \"Log\": \"notice stderr\",\n    \"ExitPolicy\": \"reject *:*\",    # not an exit node\n}\n</code></pre></p> <p><code>take_ownership=True</code> \u2014 Tor shuts down automatically if the controlling process dies.</p>"},{"location":"node/network/#_create_hidden_serviceprivate_key-bytes-hs_port-int-str","title":"<code>_create_hidden_service(private_key: bytes, hs_port: int) \u2192 str</code>","text":"<p>Converts the 32-byte Ed25519 seed to the 64-byte expanded key format Tor expects, then calls <code>controller.create_ephemeral_hidden_service()</code> with:</p> <ul> <li><code>ports={80: hs_port}</code> \u2014 external port 80 maps to internal <code>hs_port</code></li> <li><code>key_type=\"ED25519-V3\"</code></li> <li><code>await_publication=True</code> \u2014 blocks until the HS descriptor is published to the   Tor DHT and the service is reachable</li> </ul> <p>Returns <code>service_id + \".onion\"</code>.</p>"},{"location":"node/network/#_ed25519_seed_to_expandedseed-bytes-bytes","title":"<code>_ed25519_seed_to_expanded(seed: bytes) \u2192 bytes</code>","text":"<p>Derives the 64-byte expanded Ed25519 private key from a 32-byte seed. Implements RFC 8032 clamping: <pre><code>h = bytearray(sha512(seed))\nh[0] &amp;= 248\nh[31] &amp;= 127\nh[31] |= 64\n</code></pre> The resulting public key is identical to <code>nacl.signing.SigningKey(seed).verify_key</code>, ensuring the <code>.onion</code> address matches <code>identity.derive_onion_address(public_key)</code>.</p>"},{"location":"node/network/#torerror-exception","title":"<code>TorError</code> (exception)","text":"<p>Raised for any Tor startup, bootstrap, or Hidden Service creation failure.</p>"},{"location":"node/network/#nodepy","title":"node.py","text":"<p>Incoming WebSocket server. Accepts one message per connection, validates it, and dispatches it to the application handler.</p>"},{"location":"node/network/#nodeserver-class","title":"<code>NodeServer</code> (class)","text":"<pre><code>NodeServer(host: str = \"127.0.0.1\", port: int = 8765)\n</code></pre> <p>Binds exclusively to <code>127.0.0.1</code> \u2014 not accessible from the network directly. Tor's Hidden Service maps the external <code>.onion:80</code> to this port.</p>"},{"location":"node/network/#starthandler-messagehandler-none","title":"<code>start(handler: MessageHandler) \u2192 None</code>","text":"<p>Starts the websockets server. <code>handler</code> is an async callable <code>(msg: dict) \u2192 None</code> called for every valid, authenticated message.</p>"},{"location":"node/network/#stop-none_1","title":"<code>stop() \u2192 None</code>","text":"<p>Closes the server and waits for active handler coroutines to finish.</p>"},{"location":"node/network/#_handlewebsocket-handler-none","title":"<code>_handle(websocket, handler) \u2192 None</code>","text":"<p>Per-connection coroutine:</p> <ol> <li><code>await websocket.recv()</code> with 30-second timeout \u2014 idle connections are dropped</li> <li><code>parse_message(raw)</code> \u2014 invalid JSON: silent drop</li> <li><code>validate_message(msg)</code> \u2014 invalid signature/TTL/id: silent drop</li> <li><code>await handler(msg)</code> \u2014 application-level processing</li> </ol> <p><code>max_size = 12 MB</code> \u2014 accommodates 5 MB files after base64 encoding and encryption overhead.</p> <p>Any <code>ProtocolError</code> in steps 2\u20133 results in a silent drop with no response. This is intentional: not responding reveals nothing about whether the address is active.</p>"},{"location":"node/network/#messagehandler-type-alias","title":"<code>MessageHandler</code> type alias","text":"<pre><code>MessageHandler = Callable[[dict], Awaitable[None]]\n</code></pre>"},{"location":"node/network/#clientpy","title":"client.py","text":"<p>Outgoing WebSocket client. Connects to a remote <code>.onion</code> address through Tor's SOCKS5 proxy and sends a single message.</p>"},{"location":"node/network/#send_messagemsg-onion_address-socks_host-socks_port-hs_port-none","title":"<code>send_message(msg, onion_address, *, socks_host, socks_port, hs_port) \u2192 None</code>","text":"<p>Main entry point. Called by <code>DeliveryQueue._try_deliver()</code> for each delivery attempt.</p> <ol> <li>Opens a SOCKS5 connection to <code>onion_address:hs_port</code> through the Tor proxy    (<code>asyncio.wait_for</code> with 60-second timeout)</li> <li>Hands the socket to <code>websockets.asyncio.client.connect()</code></li> <li>Sends <code>json.dumps(msg)</code> over the WebSocket</li> <li>Closes the connection</li> </ol> <p>Raises <code>NodeClientError</code> on any connection or send failure.</p>"},{"location":"node/network/#socks5-implementation","title":"SOCKS5 implementation","text":"<p>A minimal SOCKS5 client is implemented from scratch to avoid introducing extra dependencies and to keep the connection fully under application control.</p>"},{"location":"node/network/#_socks5_connecthost-port-proxy_host-proxy_port-socket","title":"<code>_socks5_connect(host, port, proxy_host, proxy_port) \u2192 socket</code>","text":"<ol> <li>Opens TCP connection to the Tor SOCKS5 proxy</li> <li>Calls <code>_socks5_handshake()</code> with no-auth method</li> <li>Sets the socket to non-blocking mode</li> <li>Returns the connected socket (ready for websockets handover)</li> </ol>"},{"location":"node/network/#_socks5_handshakesock-host-port-none","title":"<code>_socks5_handshake(sock, host, port) \u2192 None</code>","text":"<p>Implements SOCKS5 protocol (RFC 1928):</p> <ol> <li>Sends greeting: <code>[VER=5, NMETHODS=1, METHOD=0 (no auth)]</code></li> <li>Verifies server accepts no-auth method</li> <li>Sends CONNECT request with <code>ATYP=DOMAIN</code> and hostname as bytes</li> <li>Reads and validates the response header (4 bytes)</li> <li>Discards the bound address (IPv4, domain, or IPv6)</li> </ol> <p>The domain name is sent as bytes \u2014 Tor resolves the <code>.onion</code> address internally via its own DHT, never using the operating system's DNS.</p>"},{"location":"node/network/#_recv_exactsock-n-bytes","title":"<code>_recv_exact(sock, n) \u2192 bytes</code>","text":"<p>Reads exactly <code>n</code> bytes from a blocking socket, looping until all bytes arrive. Raises <code>OSError</code> if the connection closes unexpectedly.</p>"},{"location":"node/network/#relaypy","title":"relay.py","text":"<p>Selects between direct delivery and relay-assisted delivery. Does not implement the relay wire protocol \u2014 that is handled by <code>client.py</code> (which sends the message to the relay's onion address like any other node).</p> <p>Relay protocol not yet active</p> <p>The relay WebSocket server (<code>legion-relay</code>) expects <code>relay_send</code>-type messages, but <code>send_via_relay()</code> currently sends standard Legion protocol messages. Relay functionality is architecturally present but not fully operational. See <code>legion-relay</code> documentation for the planned protocol.</p>"},{"location":"node/network/#functions","title":"Functions","text":""},{"location":"node/network/#is_relay_configureddb-bool","title":"<code>is_relay_configured(db) \u2192 bool</code>","text":"<p>Returns <code>True</code> if a relay is configured and enabled in the database.</p>"},{"location":"node/network/#get_relay_oniondb-str","title":"<code>get_relay_onion(db) \u2192 str</code>","text":"<p>Returns the relay's <code>.onion</code> address. Raises <code>RelayError</code> if not configured or disabled.</p>"},{"location":"node/network/#send_via_relaydb-msg-socks_host-socks_port-none","title":"<code>send_via_relay(db, msg, *, socks_host, socks_port) \u2192 None</code>","text":"<p>Sends <code>msg</code> to the configured relay node via <code>send_message()</code>. The relay is responsible for forwarding to the actual recipient.</p>"},{"location":"node/network/#choose_destinationdb-recipient_onion-tuplestr-bool","title":"<code>choose_destination(db, recipient_onion) \u2192 tuple[str, bool]</code>","text":"<p>Returns <code>(destination_onion, via_relay)</code>:</p> <ul> <li>If relay is configured and enabled: <code>(relay_onion, True)</code></li> <li>Otherwise: <code>(recipient_onion, False)</code></li> </ul> <p>Called by the API layer when enqueuing a new message to determine routing. The <code>via_relay</code> flag is stored in the delivery queue entry.</p>"},{"location":"node/overview/","title":"legion-node \u2014 Overview","text":"<p><code>legion-node</code> is the core component of Legion. It manages cryptographic identity, Tor network integration, message delivery, SQLite storage and exposes a local REST API for the GUI. It runs as a background process spawned by <code>legion-gui</code>.</p>"},{"location":"node/overview/#module-structure","title":"Module structure","text":"<pre><code>legion-node/\n\u251c\u2500\u2500 core/\n\u2502   \u251c\u2500\u2500 identity.py     \u2014 Ed25519 keypair generation, private key encryption at rest\n\u2502   \u251c\u2500\u2500 crypto.py       \u2014 signing, Box encryption, SecretBox encryption\n\u2502   \u251c\u2500\u2500 storage.py      \u2014 aiosqlite wrapper, all database operations\n\u2502   \u2514\u2500\u2500 protocol.py     \u2014 message format, construction, validation\n\u2502\n\u251c\u2500\u2500 network/\n\u2502   \u251c\u2500\u2500 tor.py          \u2014 Tor subprocess management, Hidden Service creation\n\u2502   \u251c\u2500\u2500 node.py         \u2014 incoming WebSocket server (one message per connection)\n\u2502   \u251c\u2500\u2500 client.py       \u2014 outgoing WebSocket client via Tor SOCKS5\n\u2502   \u2514\u2500\u2500 relay.py        \u2014 relay selection and routing logic\n\u2502\n\u251c\u2500\u2500 messaging/\n\u2502   \u251c\u2500\u2500 private.py      \u2014 private message send/receive, payload envelope\n\u2502   \u251c\u2500\u2500 groups.py       \u2014 group lifecycle: create, invite, post, leave, dissolve\n\u2502   \u251c\u2500\u2500 files.py        \u2014 file sanitization (Pillow), format verification\n\u2502   \u2514\u2500\u2500 delivery.py     \u2014 persistent delivery queue with 10-second retry loop\n\u2502\n\u251c\u2500\u2500 api/\n\u2502   \u2514\u2500\u2500 server.py       \u2014 FastAPI application, all endpoints, SSE stream, AppState\n\u2502\n\u251c\u2500\u2500 data/\n\u2502   \u2514\u2500\u2500 schema.sql      \u2014 SQLite schema definition\n\u2502\n\u251c\u2500\u2500 config.py           \u2014 Config dataclass, XDG-compliant data directory\n\u2514\u2500\u2500 main.py             \u2014 CLI entry point, startup/shutdown orchestration\n</code></pre>"},{"location":"node/overview/#layer-responsibilities","title":"Layer responsibilities","text":"<pre><code>main.py / config.py\n      \u2502  orchestration, CLI args, graceful shutdown\n      \u25bc\napi/server.py  \u2190\u2500\u2500\u2500\u2500 SSE events \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n      \u2502  AppState, REST endpoints               \u2502\n      \u25bc                                         \u2502\nmessaging/           network/                   \u2502\n  private.py    \u2190\u2500\u2500  node.py (incoming)         \u2502\n  groups.py     \u2500\u2500\u2500\u25ba client.py (outgoing)       \u2502\n  files.py           tor.py (Tor process)       \u2502\n  delivery.py   \u2500\u2500\u2500\u25ba relay.py (routing)         \u2502\n      \u2502                                         \u2502\n      \u25bc                                         \u2502\ncore/                                           \u2502\n  identity.py   \u2500\u2500 keypair generation           \u2502\n  crypto.py     \u2500\u2500 libsodium operations         \u2502\n  storage.py    \u2500\u2500 SQLite (aiosqlite)           \u2502\n  protocol.py   \u2500\u2500 message format/validation    \u2502\n</code></pre>"},{"location":"node/overview/#config-configpy","title":"Config (config.py)","text":"<pre><code>@dataclass\nclass Config:\n    data_dir: Path      # default: $XDG_DATA_HOME/legion or ~/.local/share/legion\n    socks_port: int = 9050    # Tor SOCKS5 proxy port\n    control_port: int = 9051  # Tor control port\n    node_port: int = 8765     # WebSocket server (Tor HS maps to this)\n    api_port: int = 8080      # FastAPI port (GUI connects here)\n    log_level: str = \"INFO\"\n</code></pre> <p>Derived properties: <code>db_path = data_dir / \"node.db\"</code>, <code>tor_data_dir = data_dir / \"tor\"</code>.</p> <p>CLI overrides (from <code>main.py</code>): <code>--data-dir</code>, <code>--api-port</code>, <code>--node-port</code>, <code>--log-level</code>, <code>--no-interactive</code> (skips password prompt, used by GUI).</p>"},{"location":"node/overview/#appstate-shared-runtime-state","title":"AppState \u2014 shared runtime state","text":"<p><code>AppState</code> (defined in <code>api/server.py</code>) is a dataclass passed to every request handler and to the message handler. It holds:</p> Field Type Purpose <code>db</code> <code>Database</code> open aiosqlite connection <code>delivery_queue</code> <code>DeliveryQueue</code> background retry loop <code>identity</code> <code>Identity \\| None</code> decrypted keypair (None until unlocked) <code>tor_onion</code> <code>str</code> active <code>.onion</code> address (empty until Tor starts) <code>tor_manager</code> <code>TorManager</code> Tor subprocess handle <code>node_port</code> <code>int</code> WebSocket server port (default 8765) <code>_tor_starting</code> <code>bool</code> Tor bootstrap in progress <code>tor_error</code> <code>str</code> last Tor error message <code>_event_queues</code> <code>list[Queue]</code> one per active SSE client <code>_log_ring</code> <code>deque</code> ring buffer of last 200 network log entries"},{"location":"node/overview/#concurrency-model","title":"Concurrency model","text":"<p>All I/O is async (<code>asyncio</code>). The event loop runs:</p> <ul> <li>uvicorn \u2014 handles HTTP requests on the main loop</li> <li>DeliveryQueue._loop \u2014 <code>asyncio.Task</code>, wakes every 10 seconds or on <code>_wake.set()</code></li> <li>NodeServer \u2014 <code>asyncio.Task</code> per incoming WebSocket connection</li> <li>Stem event listeners \u2014 run in Stem's thread; use <code>loop.call_soon_threadsafe()</code> to push into the main loop</li> </ul> <p>No <code>threading.Thread</code> is created by Legion code. Stem creates its own thread internally.</p>"},{"location":"relay/overview/","title":"legion-relay \u2014 Overview","text":"<p>Work in progress</p> <p><code>legion-relay</code> is architecturally designed and partially implemented, but the integration with <code>legion-node</code> is not yet fully operational. This page documents the intended design and current implementation status.</p>"},{"location":"relay/overview/#purpose","title":"Purpose","text":"<p>A relay node is an always-online server operated by the message sender (not a third party). It accepts encrypted message blobs from the owner, stores them, and attempts to deliver them to the recipient 24/7 \u2014 even when the sender's desktop node is offline.</p> <p>The relay is not a trusted party. It stores only encrypted blobs and never has access to the encryption keys. The recipient cannot tell whether a message was routed through a relay.</p> <pre><code>Sender node \u2500\u2500\u25ba relay node \u2500\u2500\u25ba recipient node\n   (offline)    (always on)\n</code></pre>"},{"location":"relay/overview/#what-the-relay-knows","title":"What the relay knows","text":"The relay knows The relay does not know Which sender authorized it Message content (only encrypted blobs) The recipient's <code>.onion</code> address The sender's IP address (all traffic via Tor) When messages arrive and leave Who the recipient is in real life How many retry attempts were made"},{"location":"relay/overview/#current-implementation-status","title":"Current implementation status","text":""},{"location":"relay/overview/#implemented","title":"Implemented","text":"<ul> <li><code>relay/server.py</code> \u2014 WebSocket server accepting <code>relay_send</code> and <code>relay_status</code> requests</li> <li><code>relay/storage.py</code> \u2014 SQLite wrapper for relay-specific tables</li> <li><code>relay/delivery.py</code> \u2014 retry logic and scheduling</li> <li><code>relay/cleanup.py</code> \u2014 periodic deletion of expired messages</li> <li><code>relay/tor.py</code> \u2014 Tor process management for the relay node</li> <li><code>config.py</code> / <code>main.py</code> \u2014 entry point and configuration</li> </ul>"},{"location":"relay/overview/#not-yet-operational","title":"Not yet operational","text":"<p>The integration between <code>legion-node</code> and <code>legion-relay</code> is incomplete: <code>legion-node/network/relay.py:send_via_relay()</code> currently sends standard Legion protocol messages to the relay's onion address. The relay server, however, only accepts messages of type <code>relay_send</code> with an authorization signature. This mismatch means relay delivery does not function end-to-end in the current version.</p> <p>The relay configuration can be stored in <code>legion-node</code>'s database via <code>relay_config</code>, and the <code>choose_destination()</code> function in <code>network/relay.py</code> correctly selects relay routing \u2014 but the actual relay wire protocol is not yet implemented on the sender side.</p>"},{"location":"relay/overview/#wire-protocol-relay-server-expects","title":"Wire protocol (relay server expects)","text":""},{"location":"relay/overview/#relay_send-store-and-forward-a-message","title":"<code>relay_send</code> \u2014 store and forward a message","text":"<pre><code>{\n    \"v\": 1,\n    \"type\": \"relay_send\",\n    \"sender_key\": \"&lt;hex Ed25519 public key of owner&gt;\",\n    \"auth\": \"&lt;base64 Ed25519 signature over canonical JSON without 'auth' field&gt;\",\n    \"destination_key\": \"&lt;hex public key of recipient&gt;\",\n    \"destination_onion\": \"&lt;recipient's .onion address&gt;\",\n    \"payload\": \"&lt;base64 original Legion protocol message&gt;\",\n    \"message_id\": \"&lt;SHA256 of payload&gt;\",\n    \"ttl\": 604800\n}\n</code></pre>"},{"location":"relay/overview/#relay_status-query-delivery-status","title":"<code>relay_status</code> \u2014 query delivery status","text":"<pre><code>{\n    \"v\": 1,\n    \"type\": \"relay_status\",\n    \"sender_key\": \"&lt;hex&gt;\",\n    \"auth\": \"&lt;base64 signature&gt;\",\n    \"message_id\": \"&lt;SHA256&gt;\"\n}\n</code></pre>"},{"location":"relay/overview/#authorization","title":"Authorization","text":"<p>The relay verifies: 1. <code>sender_key</code> is in the <code>authorized_senders</code> table 2. The Ed25519 signature over canonical JSON (sorted keys, no <code>auth</code> field) is valid</p> <p>Invalid signature or unauthorized sender: connection closed silently with no response.</p>"},{"location":"relay/overview/#relay-database-schema","title":"Relay database schema","text":"<pre><code>CREATE TABLE relay_identity (\n    public_key    TEXT PRIMARY KEY,\n    private_key   BLOB NOT NULL,\n    onion_address TEXT NOT NULL,\n    created_at    INTEGER NOT NULL\n);\n\nCREATE TABLE authorized_senders (\n    public_key TEXT PRIMARY KEY,\n    alias      TEXT,\n    added_at   INTEGER NOT NULL\n);\n\nCREATE TABLE stored_messages (\n    id                TEXT PRIMARY KEY,\n    sender_key        TEXT NOT NULL,\n    for_key           TEXT NOT NULL,\n    destination_onion TEXT NOT NULL,\n    payload           BLOB NOT NULL,\n    received_at       INTEGER NOT NULL,\n    expires_at        INTEGER NOT NULL,\n    next_retry_at     INTEGER NOT NULL,\n    retry_count       INTEGER DEFAULT 0,\n    status            TEXT NOT NULL CHECK (status IN ('queued', 'delivered', 'failed'))\n);\n</code></pre>"},{"location":"relay/overview/#configuration-defaults","title":"Configuration defaults","text":"Parameter Default Description <code>MAX_MESSAGE_SIZE_KB</code> 512 Maximum accepted message size <code>MAX_STORED_MESSAGES</code> 10000 Hard cap on queue depth <code>MAX_TTL_DAYS</code> 30 TTL capped at 30 days regardless of request <code>DEFAULT_TTL_DAYS</code> 7 Default TTL if not specified <code>CLEANUP_INTERVAL_SECONDS</code> 3600 Cleanup runs every hour <code>DATA_DIR</code> <code>~/.local/share/legion-relay/</code> Data directory"},{"location":"relay/overview/#deployment","title":"Deployment","text":"<p>The relay is a standalone Python process intended to run on a server (VPS, home server) with a stable internet connection. It creates its own Tor Hidden Service with its own Ed25519 keypair, independent of any user's identity.</p> <p>After first run, the operator notes the relay's <code>.onion</code> address and public key, then enters these in <code>legion-node</code>'s Settings \u2192 Relay section.</p>"}]}